package msr.mirudl.shared

/**
 * Placeholder for Phase 0.1 — proves the :shared module and its
 * commonMain source set compile successfully for every configured
 * target (android, desktop) before any real logic is moved here.
 * See MIGRATION_PLAN.md.
 */
object SharedInfo {
    const val MODULE_NAME = "shared"
}
